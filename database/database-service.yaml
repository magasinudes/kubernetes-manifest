apiVersion: "v1"
kind: "Service"
metadata:
  name: "database-service"
  namespace: "default"
  labels:
    app: database
spec:
  ports:
  - protocol: "TCP"
    port: 5432
    targetPort: 5432
  selector:
    app: "database"
  type: "LoadBalancer"
  loadBalancerIP: ""