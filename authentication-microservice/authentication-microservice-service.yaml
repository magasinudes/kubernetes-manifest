apiVersion: "v1"
kind: "Service"
metadata:
  name: "authentication-service"
  namespace: "default"
  labels:
    app: authentication
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8080
  selector:
    app: "authentication"
  type: "LoadBalancer"
  loadBalancerIP: ""