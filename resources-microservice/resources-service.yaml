apiVersion: "v1"
kind: "Service"
metadata:
  name: "resources-service"
  namespace: "default"
  labels:
    app: "resources"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8080
  selector:
    app: "resources"
  type: "LoadBalancer"
  loadBalancerIP: ""
